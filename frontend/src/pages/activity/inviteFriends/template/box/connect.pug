- var header_url = img_header
- var footer_url = img_footer
-var connect_url = img_connect
section.box(data-name='myPrize')
    .coupon_float
    img.box_header(src=header_url)
    div.title #{title}
    div.box_body
        !=box_body
    img.box_connect(src=connect_url)
    div.box_body
        !=box_body_2
    img.box_footer(src=footer_url)

